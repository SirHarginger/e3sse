---
agent: agent
description: Prepare a reproducible E3-SSE scientific analysis run
---

Use the `experiment-run` skill.

Inspect the requested analysis, configuration, input datasets, model identity,
split, seed and expected output.

Perform a smoke test before proposing a production run.

Do not write to `data/`.
